import { Routes } from '@angular/router';
import { DataComparisonComponent } from './data-comparison/data-comparison.component';

export const routes: Routes = [{
    path: '', component: DataComparisonComponent,
},];
